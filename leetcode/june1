class Solution {
public:
    int scoreOfString(string s) {
        int n = s.length();
        int sum = 0;
        for (int i = 0; i < n-1; i += 1) {
            int a = abs(int(s[i]) - int(s[i + 1]));
            sum += a;
        }
        return sum;
    }
};
